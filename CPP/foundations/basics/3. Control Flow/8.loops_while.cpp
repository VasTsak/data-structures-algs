#include <iostream>

int main()
{
    int entry = 0;

    // condition is True

    while (entry <= 5)
    {
        std::cout << "incrementing entry = " << entry << "\n";
        entry++;
    }
    return 0;
}